<%= form_for(@midia) do |f| %>
  <% if @midia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@midia.errors.count, "error") %> Não foi possível salvar essa mídia:</h2>

      <ul>
      <% @midia.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
